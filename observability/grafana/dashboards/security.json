{
  "annotations": { "list": [] },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(sre_agent_scan_findings_total{scanner=\"gitleaks\"}[30m])) by (severity)",
          "legendFormat": "gitleaks {{severity}}",
          "refId": "A"
        }
      ],
      "title": "Gitleaks Findings (rate)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 10, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(sre_agent_scan_findings_total{scanner=\"trivy\",severity=~\"HIGH|CRITICAL\"}[30m])) by (severity)",
          "legendFormat": "trivy {{severity}}",
          "refId": "A"
        }
      ],
      "title": "Trivy HIGH/CRITICAL Findings (rate)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 10 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(sre_agent_scan_fail_total[30m])) by (scanner, reason)",
          "legendFormat": "{{scanner}} {{reason}}",
          "refId": "A"
        }
      ],
      "title": "Scan Failures (rate)",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "refresh": "30s",
  "tags": ["sre-agent"],
  "templating": { "list": [] },
  "time": { "from": "now-24h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Security",
  "uid": "sre-agent-security",
  "version": 1
}

